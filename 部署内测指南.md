# 课游工坊 Vercel 内测部署指南

当前版本不使用账号和数据库。教师制作的课游只保存在其浏览器本地；Vercel Function 仅负责隐藏 TokenRouter Key 并转发 AI 出题请求。

## 一、准备 TokenRouter 配置

在 TokenRouter 控制台确认：

1. API Key；
2. OpenAI 兼容 Base URL，例如 `https://示例地址/v1`；
3. 模型准确名称，例如控制台提供的 DeepSeek 模型名。

不要把 API Key 写进代码、发到聊天或提交到 GitHub。

如果控制台提供的是完整地址，例如 `https://示例地址/v1/chat/completions`，使用 `WORKSHOP_AI_CHAT_URL`；不要同时把这段完整地址当成 Base URL，否则会重复拼接路径。

## 二、上传 GitHub

1. 在 GitHub 创建私人仓库；
2. 把项目文件上传到仓库根目录；
3. 确认没有上传 `.env` 或 `.env.local`；
4. `vercel.json`、`api/index.mjs`、`lib/generator.mjs` 必须保留。

## 三、部署到 Vercel

1. 登录 Vercel，选择 **Add New → Project**；
2. 导入刚才的 GitHub 仓库；
3. Framework Preset 选择 **Other**；
4. Root Directory 保持项目根目录；
5. Build Command 和 Output Directory 都留空；
6. 在 Environment Variables 添加下面的变量。

```text
WORKSHOP_AI_API_KEY=你的TokenRouter Key
WORKSHOP_AI_BASE_URL=TokenRouter提供的Base URL
WORKSHOP_AI_MODEL=TokenRouter提供的准确模型名
WORKSHOP_AI_JSON_MODE=true
```

如果拿到的是完整请求地址，则增加：

```text
WORKSHOP_AI_CHAT_URL=https://示例地址/v1/chat/completions
```

完成后点击 **Deploy**。

## 四、上线检查

部署完成后依次检查：

1. 打开 `https://你的域名/api/health`，应看到 `{"ok":true}`；
2. 打开首页，应显示“智能出题服务已连接”；
3. 输入一个知识点并生成题目；
4. 完成预览后进入“我的课游”，确认能看到本机记录；
5. 下载 HTML 后断网双击，确认游戏仍能运行。

如果 TokenRouter 返回“不支持 `response_format`”，把 Vercel 环境变量改为：

```text
WORKSHOP_AI_JSON_MODE=false
```

修改环境变量后需要重新部署。

## 五、本地缓存须知

- 不同设备、不同浏览器之间不会同步作品；
- 清除浏览器网站数据后，本机作品记录也会消失；
- 建议老师及时下载最终 HTML，下载文件不依赖网站和网络；
- 浏览器只保留最近 20 份制作记录；
- Vercel 不保存题库、作品、学生信息或互动数据。

## 六、内测成本控制

- Vercel 和 TokenRouter 的免费额度、计费规则以各自控制台为准；
- 当前接口对单个来源设置了基础生成频率限制；
- 建议在 TokenRouter 设置余额预警或消费上限；
- 第一轮只邀请少量教师，并暂时不要公开传播网址。
